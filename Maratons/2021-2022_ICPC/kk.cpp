#include <bits/stdc++.h>
#define int long long
using namespace std;

signed main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin >> n;
    vector<int> buffer;
    int cont = 0;
    int tempo = 0;
    vector<pair<int, int>> people(n);
    for(int i = 0; i < n; i++){
        int t, d;
        cin >> t >> d;
        people.push_back({t, d});
        

    }

}